# ===== REQUIRED =====

# Database connection string (PostgreSQL)
# Railway will automatically provide this if you connect a PostgreSQL service
DATABASE_URL="postgresql://user:password@localhost:5432/taskflow?schema=public"

# NextAuth.js secret - generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-min-32-chars"

# Application URL
# For Railway: use your Railway domain (e.g., https://your-app.up.railway.app)
# For local development: http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# ===== RAILWAY SPECIFIC =====

# Railway sets these automatically, but you can override if needed
# PORT=8080
# HOSTNAME=0.0.0.0

# ===== OPTIONAL =====

# Email configuration (for invite notifications - can be added later)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=app-specific-password
